const crypto = require('crypto');
const meals = require('../models/mealModel');
function validate(body) { const meal = { name: String(body.name || '').trim(), price: Number(body.price), cuisine: String(body.cuisine || '').trim(), diet: String(body.diet || '').trim(), availability: String(body.availability || '').trim(), image: String(body.image || '').trim() }; if (meal.name.length < 2 || !Number.isFinite(meal.price) || meal.price <= 0 || meal.price > 100000 || !meal.cuisine || !['Veg','Non-Veg','Vegan'].includes(meal.diet) || !['Available','Sold out','Unavailable'].includes(meal.availability) || (meal.image && !/^https?:\/\//i.test(meal.image))) return null; return meal; }
exports.list = async (_req, res) => res.json({ ok: true, meals: (await meals.all()).filter(meal => meal.availability !== 'Unavailable') });
exports.mine = async (req, res) => res.json({ ok: true, meals: await meals.byCook(req.user.id) });
exports.create = async (req, res) => { const values = validate(req.body); if (!values) return res.status(400).json({ ok:false, message:'Provide a valid name, positive price, cuisine, diet, availability, and optional http(s) image URL.' }); const meal = await meals.create({ id: crypto.randomUUID(), ...values, cookId: req.user.id, cookName: req.user.username, createdAt: new Date().toISOString() }); res.status(201).json({ ok:true, meal }); };
exports.update = async (req, res) => { const current = await meals.find(req.params.id); if (!current) return res.status(404).json({ ok:false, message:'Meal not found.' }); if (current.cookId !== req.user.id) return res.status(403).json({ ok:false, message:'You can only edit your own meals.' }); const values = validate(req.body); if (!values) return res.status(400).json({ ok:false, message:'Meal details are invalid.' }); res.json({ ok:true, meal: await meals.update(current.id, values) }); };
exports.remove = async (req, res) => { const current = await meals.find(req.params.id); if (!current) return res.status(404).json({ ok:false, message:'Meal not found.' }); if (current.cookId !== req.user.id) return res.status(403).json({ ok:false, message:'You can only delete your own meals.' }); await meals.remove(current.id); res.json({ ok:true }); };
